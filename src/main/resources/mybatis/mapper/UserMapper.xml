<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.shopping.infrastructure.persistence.mybatis.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password_hash, role, created_at)
        VALUES (#{email}, #{passwordHash}, #{role}, #{createdAt})
    </insert>

    <select id="findByEmail" parameterType="string" resultType="project.shopping.domain.user.model.User">
        SELECT id, email, password_hash, role, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM users
        WHERE email = #{email}
    </select>

</mapper>
