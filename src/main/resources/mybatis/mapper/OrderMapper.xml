<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.shopping.infrastructure.persistence.mybatis.mapper.OrderMapper">

    <insert id="insertOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (customer_id, status, total_price, created_at)
        VALUES (#{customerId}, #{status}, #{totalPrice}, #{createdAt})
    </insert>

    <insert id="insertItems">
        INSERT INTO order_items (order_id, product_id, quantity, unit_price)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{orderId}, #{item.productId}, #{item.quantity}, #{item.unitPrice})
        </foreach>
    </insert>

    <select id="findById" resultType="project.shopping.domain.order.model.Order">
        SELECT id, customer_id, status, total_price, created_at
        FROM orders
        WHERE id = #{orderId}
    </select>

    <select id="findByIdAndCustomerId" resultType="project.shopping.domain.order.model.Order">
        SELECT id, customer_id, status, total_price, created_at
        FROM orders
        WHERE id = #{orderId} AND customer_id = #{customerId}
    </select>

    <select id="findAllByCustomerId" resultType="project.shopping.domain.order.model.Order">
        SELECT id, customer_id, status, total_price, created_at
        FROM orders
        WHERE customer_id = #{customerId}
        ORDER BY created_at DESC
    </select>

    <select id="findItemsByOrderId" resultType="project.shopping.domain.order.model.OrderItem">
        SELECT id, order_id, product_id, quantity, unit_price
        FROM order_items
        WHERE order_id = #{orderId}
        ORDER BY id ASC
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status}
        WHERE id = #{orderId}
    </update>

</mapper>
